# Copy to .env on docker-01 and fill in a real password. .env is gitignored — it holds a
# database credential that doesn't belong in the repository.

POSTGRES_USER=mlflow
POSTGRES_PASSWORD=change-me
POSTGRES_DB=mlflow
